- hosts: all
  become: yes
  become_user: Administrator
  tasks: 
    - name: run multi-lined shell commands
      community.general.parted:
        device: /dev/sdi
        number: 2
        state: present
        fs_type: ntfs
