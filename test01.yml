- hosts: all
  gather_facts: true
  tasks:
    - ping:
    - copy:
        content: " export {{ ansible_env.INSTANCE_HOSTNAME }} = {{ ansible_env.IP_INTERNAL }} "
        dest: /tmp/test
      delegate_to: 10.0.0.67
      run_once: true
      become: true
