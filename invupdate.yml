
- hosts: app
#  vars:
#    IP: "{{ ansible_env.IP_INTERNAL }}" 
#    HOSTNAME: "{{ ansible_env.INSTANCE_HOSTNAME }}" 
  gather_facts: true
  tasks:
    - ping:
    - debug:
        msg: "System IP {{ ansible_env.IP_INTERNAL }} has hostname {{ ansible_env.INSTANCE_HOSTNAME }}" 
    - name: Echo my_env_var
      shell: "echo $MY_ENV_VARIABLE"
      environment:
        PATH: "{{ansible_env.INSTANCE_HOSTNAME}}:{{ansible_env.IP_INTERNAL}}" 

    - name: Echo my_env_var again
      shell: "echo $MY_ENV_VARIABLE" 
